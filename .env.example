# Copy this file to `.env` (which is gitignored) and fill in your keys.
#   cp .env.example .env
# Never commit `.env`.

# setlist.fm API — historical concerts (the event source for the PeMS training window).
# Free key: https://api.setlist.fm/docs/1.0/index.html  (Apply for an API key)
SETLISTFM_API_KEY=
